# 变更日志

## 2026-08-16

### 交易与优惠券

- 订单接口改为强类型请求对象，并增加字段校验和幂等键。
- 下单链路使用 Redis 带租约锁、MySQL 联合唯一索引和服务端历史订单查询处理重复请求。
- 套餐库存改为库存条件与版本号联合原子更新。
- 订单金额统一由服务端根据套餐和最优优惠券计算。
- 新人券增加来源唯一约束，防止重复领取。

### 消息与通知

- 订单事务写入 Outbox 事件，事务提交后发布至 RabbitMQ。
- 消费端增加消息幂等、有限重试、手动 ACK 和死信处理。
- 异步生成 16 位订单券码，并统一处理邀请奖励和站内订单通知。
- 增加死信积压查询与单条重放接口。

### 用户与会话

- 注册成功后直接建立登录 Session。
- 登录密码使用 BCrypt 校验，退出登录销毁服务端 Session。
- Redis Session 支持两个后端实例共享登录状态。
- 请求参数校验失败统一返回 `400 Bad Request`。

### 缓存与可观测性

- 商家和套餐使用 Cache-Aside 缓存，并增加空值缓存、随机过期、写后失效和数据库回退并发控制。
- 附近商家使用 Redis GEO 按品类、半径和距离检索。
- 增加请求 ID、Logstash JSON 结构化日志、Actuator 指标和接口限流。

### 前端与部署

- 前端构建从 Vue CLI 迁移到 Vite，删除 Mock 接口和未使用依赖。
- Axios 改为同源相对请求，开发环境通过 Vite 代理访问真实后端。
- 下单请求增加随机幂等键，券码页面轮询异步生成结果。
- 增加订单通知页面和退出登录接口调用。
- 后端和前端均采用多阶段 Docker 镜像构建，Nginx 代理两个后端实例。
- Docker Compose 增加本地演示数据初始化。

### 验证

- 后端 19 项自动化测试通过。
- 前端生产构建通过，npm 依赖审计结果为 0 个已知漏洞。
- Docker Compose 全链路验证通过，包括重复请求幂等、单次库存扣减、异步券码、订单通知和多实例 Session 故障转移。
